{ config, pkgs, ... }:
{
  programs.bash = {
    enable = true;
    # shellAliases = shellAliases;
  };

  programs.zsh = {
    enable = true;
    # shellAliases = shellAliases;
  };

  programs.fish = {
    enable = true;
    # shellAliases = shellAliases;
  };


}